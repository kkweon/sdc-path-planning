#ifndef VARIABLES_HPP
#define VARIABLES_HPP

namespace env {
// MAX LENGTH MAP
const double MAX_S = 6945.554;
// MAX SPEED LIMIT
const double MAX_VELOCITY = 49.5;
// There are 3 lanes available
const int NUMBER_OF_LANES = 3;
// Each lane is 4 meter wide
const int LANE_WIDTH = 4;

  const double UPDATE_FREQ = .02;
}

#endif
